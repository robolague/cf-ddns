Dynamic DNS client for CloudFlare (One Zone)
I use this in k8s as a cronjob

Requires the following ENV variables and/or secrets:

Docker image at https://hub.docker.com/r/robolague/cf-ddns

BASEID
ZONE
AUTH_EMAIL
AUTH_KEY
BEARER

also requires a configmap to /etc/ddupdate/dnslist